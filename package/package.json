{
  "name": "companion-package",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-sidecar": "test -x tauri/binaries/companion-server-aarch64-apple-darwin || (cp tauri/binaries/companion-server.sh tauri/binaries/companion-server-aarch64-apple-darwin && chmod +x tauri/binaries/companion-server-aarch64-apple-darwin)",
    "tauri": "tauri",
    "dev": "bun run prepare-sidecar && tauri dev",
    "build": "bun run prepare-sidecar && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}
